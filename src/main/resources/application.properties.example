# ===============================
# CONFIGURAZIONE DATABASE POSTGRES
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/ndovado_db
spring.datasource.username=postgres
spring.datasource.password=LA_TUA_PASSWORD_POSTGRES
spring.datasource.driver-class-name=org.postgresql.Driver
logging.level.org.springframework.jdbc.core=DEBUG
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.springframework.transaction=TRACE

# ===============================
# API KEYS ESTERNE
# ===============================
# Genera una chiave gratuita su Google AI Studio: https://aistudio.google.com/app/apikey
gemini.api.key=LA_TUA_CHIAVE_GEMINI
# Nessuna API key necessaria per la mappa: usa Leaflet + OpenStreetMap (gratuito)

# ===============================
# EMAIL (Gmail SMTP)
# ===============================
# 1) Abilita la Verifica in 2 passaggi sul tuo account Google
# 2) Vai su: Account Google -> Sicurezza -> Password per le app
# 3) Genera una password per "Posta" e incollala in spring.mail.password
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=LA_TUA_EMAIL@gmail.com
spring.mail.password=LA_TUA_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# URL frontend (usato nei link delle email)
app.frontend.url=http://localhost:4200

# ===============================
# SPRING MVC / UPLOAD FILE
# ===============================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
